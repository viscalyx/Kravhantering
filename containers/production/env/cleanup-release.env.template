# Keep this selection separate from release.env. Change it only through the
# explicit cleanup update procedure with a verified release contract.
TRANSIENT_CLEANUP_IMAGE_REF=registry.example.internal/kravhantering-db-job:replace-with-cleanup-release-tag
