interface:
  display_name: "Operator Upgrade Notes"
  short_description: "Review PR diffs for operator upgrade notes"
  default_prompt: "Use $operator-upgrade-notes to review committed, staged, unstaged, and untracked local changes against main (falling back to origin/main) and draft any high-level operator upgrade notes needed for the PR."
policy:
  allow_implicit_invocation: false
