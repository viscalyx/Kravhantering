interface:
  display_name: "Operator Upgrade Notes"
  short_description: "Identify PR operator upgrade impacts"
  default_prompt: "Use $operator-upgrade-notes to inspect this branch's diff against main and draft PR operator upgrade notes when operations are affected."
policy:
  allow_implicit_invocation: false
