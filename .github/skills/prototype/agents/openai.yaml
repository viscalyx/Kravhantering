interface:
  display_name: "Prototype"
  short_description: "Build throwaway logic or UI prototypes"
  default_prompt: "Use $prototype to build a throwaway logic or UI prototype that answers a specific design question before committing to production code."
policy:
  allow_implicit_invocation: false
