interface:
  display_name: "Prune Superseded ADRs"
  short_description: "Keep ADRs limited to current decisions"
  default_prompt: "Use $prune-superseded-adrs to audit every ADR, change only superseded decisions or incorrect domain language, inspect code only where ADRs contradict, and keep all other wording and formatting unchanged."
policy:
  allow_implicit_invocation: false
