interface:
  display_name: "Caveman Mode"
  short_description: "Ultra-compressed communication mode. Cuts token usage ~75% by dropping filler while keeping technical accuracy."
  default_prompt: "Use $caveman to convert the user's request into a concise, technical prompt that minimizes token usage while retaining full meaning."
policy:
  allow_implicit_invocation: false
