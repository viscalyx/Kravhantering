# Keep production secrets and deployment URLs in the deployment secret store.
# Use .env.prodlike (committed) for local prod-like validation defaults.
NEXT_PUBLIC_ENABLE_REQUIREMENT_DETAIL_PREFETCH=true
